#pragma once

#include<vector>
class Functions
{
public: 
	static int f(int x);

	static int transitiveFunction(int x);

	static int g(int x, int floatNumValue);

	static int logicF(int x);

	static double kakutani_neumann(int num);
	static double monna_map(int n);
};

